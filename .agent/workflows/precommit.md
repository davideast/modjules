---
description: Run pre-commit verification checks (format, type-check, test)
---

// turbo-all

1. Run the pre-commit script to verify everything: `npm run precommit`
2. If CI fails on attribution, run: `bun scripts/fix-attribution.ts`
